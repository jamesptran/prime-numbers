## How to try out this implementation:
First compile it:

```bash
g++ work-optimized.cpp -o prime_numbers
```

Then run:

```bash
./prime_numbers
```

> Only available on machines with gcc compiler installed.
